# Pre requisites
    - Docker daemon running
    . env file in root folder

# Env file

The env file shall contain the db connection data

```
POSTGRES_DB=[your_db_name]
POSTGRES_USER=[your_db_user]
POSTGRES_PASSWORD=[your_db_password]
```
